<!DOCTYPE html>
<html>
  <head>
    <title>EventBriteLike</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
    <div class="container-flex">
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/">The-Gossip-Project</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href=<%= teams_path%>>Team</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href=<%= contacts_path%>>Contact</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href=<%= users_path%>>Users</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href=<%= cities_path%>> Cities</a>
      </li>
            <li class="nav-item">
        <a class="nav-link" href=<%= new_gossip_path%>>New Gossip</a>
      </li>

<% if logged_in? %>
      <li class="nav-item">
        <a class="nav-link"> <%= button_to "User : #{current_user.email}", user_path(current_user.id), method: :get %> </a>
      </li>
      <li class="nav-item">
        <a class="nav-link"> <%= button_to "Se déconnecter", session_path(session.id), method: :delete %></a>
      </li>
<% else %>
      <li class="nav-item">
        <%= button_to "Login", new_session_path, method: :get%>
      </li>
      <li class="nav-item">
        <%= button_to "Sign Up", new_user_path, method: :get%>
      </li>


<%end%>
  </div>
</nav>
</div>
<% flash.each do |key, value| %>
<div id="close" class="<%= flash_class(key) %>" role="alert">
  <%= value %>
</div>
<% end %>

  <body>
    <%= yield %>
  </body>
</html>
